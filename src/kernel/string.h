#pragma once
#include "stdio.h"
#include <stdint.h>

const char* strchr(const char* str, char chr);
char* strcpy(char* dst, const char* src);
unsigned strlen(const char* str);
uint16 strlength(string ch);//new
uint8 strEql(string ch1,string ch2);//new

